// Sensible limits for inputs
export const speedRegEx = /^(?![0])[0-9]{0,3}$/
export const distanceRegex = /^(?![0])[0-9]{0,4}$/

// /^([1-9]{0,4})$/

// /^(?![0])[0-9]{0,4}$/

// /^(?![0])[0-9]{0,4}$/
